# 固定角色参考音频工作流

用户要求：先为每个角色制作符合设定的独立测试朗读，后续正式音频通过 TA2A 传入这些固定参考音频，并在 Prompt 中明确标注角色与音频的对应关系。

## 当前样片角色

| 角色 | 固定参考文件 | 正式请求标记 |
| --- | --- | --- |
| 女旁白 | output/audio/ta2a/narrator-reference.wav | @音频1 |
| 蝙蝠侠 | output/audio/ta2a/batman-reference.wav | @音频2 |
| 冒充小丑的囚犯 | output/audio/ta2a/impostor-reference.wav | @音频3 |

前两位覆盖第一段；第二段新增冒充小丑的囚犯。囚犯的独立试音按文字设定生成，尚未使用图片参考；正式第二段通过TA2A同时传入三条参考音频。新增角色时先制作新的独立参考录音，再纳入角色表；不为沉默角色添加对白。真正的小丑需另外选角，不复用替身的音色。

## 角色音色设计：文字与参考图

依据用户提供的第二张提示词截图，按整体时间顺序描述音频，明确区分人声、穿插音效和 BGM。用稳定的角色名或代称区分所有说话人；人物描述放在 Prompt 最前方或该角色第一次出现的位置。描述写在角色名后的括号内，并明确不朗读说明文字。角色说话方式发生变化时，在对应台词处补充说明。

人物描述应覆盖年龄设定、性别设定、角色属性、口音、情绪和声线（例如磁性、气泡音、沙哑），必要时补充语速、起伏和人设。这些是创作设定，不是通过图片对真实人物作出的身份或属性判断。

对于漫画角色，可选取能清楚表现角色形象、表情和气质的原画作为参考图，辅助设计声音。单个角色独立生成一条干净的短测试朗读，选定后固定为该角色的音频参考；后续正式片段传入固定录音，并在 Prompt 中明确绑定 @音频N。角色图片文件与参考音频一起保存，便于后续追溯选角设定。

图片参考阶段的接口要求（已在官方音频生成HTTP文档核对）：references 中传 image_data（Base64）或 image_url，两者互斥；最多1张，支持 jpeg/png/webp，不超过10MB。同一次请求不能把图片参考与 audio_data、audio_url 或 speaker 混用。因此分为两步：图片参考生成角色测试录音；选定录音后用音频参考进行 TA2A 正式生成。

图片参考负责提供角色视觉信息，生成的音色与表现需要试听选择。后续复用已选定的录音，不为每段重新从图片生成角色声音。此前已有的两个角色参考是通过音色ID和文字设定生成的，尚未使用图片参考。

蝙蝠侠的图片选角 Prompt 示例（需在请求中另外传入角色图片；此处年龄和声音属于本项目拟定的表演设定）：

```text
参考所附漫画角色图片，为蝙蝠侠设计一条独立的中文角色测试录音。
蝙蝠侠（成年男性，约35—45岁，普通话；低沉厚实的胸腔音，略带沙哑但吐字清楚；警觉、克制，语速略慢，严肃而不夸张）说道：“先别开灯。告诉我，你看见了什么。慢慢说，我在听。”
只朗读引号内文字，角色名和括号内描述不朗读。只有一个男声，录音干净，不加音乐、环境音或音效，约10秒。
```

人物对白与环境声音联合生成时，可以按“BGM开始 → 人物A描述与台词 → 音效 → 人物B描述与台词 → 音效”的时间顺序撰写。是否联合生成或拆成独立音轨，按片段的叙事与后期控制需求决定；角色参考录音始终保持干净人声。

用户在首版视频反馈中明确要求更大胆地使用音效和BGM，并再次提供了连续声音叙事的提示词示例。后续正式场景优先将BGM的进入、发展、收弱和退场，与动作音效及角色对白交错写入同一份Prompt；不要将音效集中在末尾罗列。雨打铁门、靴子脚步等重要叙事音效必须要求清楚可辨，避免反复用“极轻、轻微、压低”导致生成结果缺少表现力。对白清晰与音效存在感需同时兼顾，在对白空隙让动作声更突出。

第二段的表演反馈：蝙蝠侠与替身需要更生动、更贴合原画冲突的对白。固定参考音频用于声音身份，不能将参考试音的平稳语气固定为所有正式场景的表演。按动作明确声音变化：蝙蝠侠由压抑劝说、迫切、不耐烦升级到暴怒；替身由受惊、急切抗议、虚张声势变成慌乱与泄气。逐句注明重音、咬字、呼吸、快慢及音量变化，避免整段匀速朗读。用户选择整段重新生成来保持情绪发展，而非只替换最后一句。对应新提示词：`output/audio/ta2a/scene-02-performance-v2-prompt.txt`。

用户试听发现表演强化版中替身抗议与末句逼问的实际咬字不符合台词，尽管生成返回的字幕看起来正确。不能以返回字幕一致宣称实际朗读无误。新尝试 `scene-02-diction-v3-prompt.txt` 取消逐句时间窗口与逐字拖长嘶吼要求，优先完整、准确咬字，采用自然对白节奏后再对齐镜头和拟音。时间窗口或强情绪是否为错读原因仍未证实。新增本地 `scripts/local_asr_check.py` 用无预设台词的独立识别辅助检查；识别也可能误判，仍需试听评价。

本次对应提示词为 `output/audio/ta2a/scene-cinematic-v2-prompt.txt`：雨打铁门与低弦乐开场，走廊靴声接钥匙门锁，对峙期间配乐缓慢增强，问句结束后收弱，纸牌落桌收尾。继续传入原女旁白和蝙蝠侠的两条独立音色参考。

## 根据漫画画面穿插音效

用户希望正式生成 Prompt 中直接穿插丰富的画面音效。阅读相关画格后，按画格动作和叙事顺序将声音写在相邻台词之间，必要时说明与台词轻微重叠；不要只在结尾笼统写“加入丰富音效”。正式场景默认可联合生成人声与音效，角色参考录音仍保持干净，后期需要精确控制时再独立生成补充音轨。

- 从画面寻找声音依据：天气、可见动作、接触材质、空间转换以及漫画拟声字。画面未明确发声但动作自然产生的声音可作为拟音，例如披风摩擦、钥匙轻碰；不能把推测的声音当作原作新增剧情。
- 每个重要音效描述声音来源、材质、远近、强弱、出现时机和持续长度。例如“对白结束后的半秒，纸牌在木桌上短促地轻拍一下”。
- 环境声负责空间连续性；动作声负责呈现行为；停顿保留情绪。用少量重点声音引导注意力，不让所有物体同时发声。
- 对白期间压低背景，关键音效优先放在自然停顿或镜头切换处。音色参考标记和台词时间区间照常保留，音效说明不朗读。
- 依据本漫画PDF第5—8页，开篇可用雨打水洼和铁门、涉水脚步、室内脚步短回声、披风摩擦、钥匙与门锁、椅脚摩擦、抽牌和纸牌拍桌。门锁、布料等为画面动作的拟音设计；纸牌拍桌另有画面拟声字支持。不要无依据添加枪声、爆炸、雷击或人物笑声。

当前 scene-timed-prompt.txt 已由纯人声草稿改为联合人声、环境声和动作音效的30秒提示词，并已用于生成 batman-timed-sfx-sample.wav（30秒）。它是 output/video/batman-killing-joke-pilot.mp4 的音轨；已有样片的原始 scene-prompt.txt 保持不变。

## 调用约定

- 首次制作角色参考，可以用音色 ID 配合角色表演提示词生成干净人声测试朗读。
- 正式生成必须传入固定参考文件的 audio_data 或 audio_url；不得仅传 speaker ID 来替代这个步骤。
- 参考音频以 references 数组顺序编号。每句台词明确写出对应 @音频N，且说明标记和表演指令不朗读。
- 固定参考文件一旦选定，后续持续复用原始字节。变更音色应另存版本，不覆盖原角色参考。
- 当前官方接口限制：最多3条参考音频，每条不超过30秒、10MB。超过3个角色的场景按出演角色分组生成，并为每次请求记录编号映射。
- 文件和字幕检查不等于音色验收。应单独检查参考音色、正式片段里的角色切换，以及与各自参考的听感一致性；声学指标只提供辅助证据。

## 当前正式生成示例

在项目根目录运行，脚本从 SEED_AUDIO_API_KEY 环境变量读取密钥；没有环境变量时从标准输入读取一行。密钥不写入项目文件。

```bash
python3 scripts/seed_audio_generate.py \
  --prompt output/audio/ta2a/scene-prompt.txt \
  --reference output/audio/ta2a/narrator-reference.wav \
  --reference output/audio/ta2a/batman-reference.wav \
  --format wav \
  --output output/audio/ta2a/batman-ta2a-sample.wav
```

该命令会产生新的计费请求。正式样片为一次模型生成，不拼接两个单角色输出。相邻 JSON 保存字幕、请求 ID 及实际传入参考文件的 SHA-256，便于追溯。

官方接口文档：https://docs.volcengine.com/docs/6561/2550782?lang=zh

## 台词时间区间与视频分镜

依据用户提供的提示词截图：在“说道：”之后、实际台词之前使用 `[2.7s:5.7s]` 这样的时间标记。冒号前为开始时间，后为结束时间，单位是秒，可使用小数。所有时间均按当前生成片段从0秒计时；拼接到整片时另加片段偏移。

与角色参考绑定一起使用，例如：

```text
蝙蝠侠（沿用@音频2的低沉男声）冷静地说道：[9.5s:13.2s]“你好。我来，是想和你谈谈。”
```

先根据分镜为对白分配合理的时间区间，给角色切换、无对白画面和音效留出空隙，再生成音频。时间标记是生成控制目标，不应当作已验证的精确剪辑结果；用返回的逐字时间戳检查实际开始、结束时间，再调整视频切点，必要时局部重生成。不要为了硬塞进时间窗口而让长台词异常加速。

带时间标记的30秒样片提示词：output/audio/ta2a/scene-timed-prompt.txt。已调用生成并用于首段视频。实际台词相对目标区间存在少量偏差，视频使用返回的逐字时间戳校准；此前旧音频样片未使用这种显式时间标记。

Seed Audio 也可按用户要求单独生成无人声环境音、动作音效或 BGM。可以独立制成音轨，按视频时间线放置、调音量及淡入淡出；台词使用以上时间区间写法，音效和音乐的起止、持续时长及变化另用自然语言明确描述。台词、音效、音乐均需检查实际输出与目标时间线的偏差。

## 生成字幕

默认使用 Seed Audio 的 audio_config.enable_subtitle=true，直接保存 subtitle.sentences[].words[] 的逐字/词时间戳（毫秒），输出 JSON、SRT 或 VTT。只为了生成字幕不需要额外 ASR；独立语音识别用于检查实际朗读内容和说话人。

## 独立语音识别与字幕

MiniMax ASR 接入脚本：scripts/minimax_transcribe.py。使用 asr-1.0、response_format=verbose_json、timestamp_level=word、stream=false，返回中文逐字、英文逐词时间戳和匿名说话人编号。

需要本地环境变量 MINIMAX_API_KEY，按 Authorization: Bearer 鉴权。用户提供的 GID 不能代替 API Key；此 STT 接口文档未列出 GID 参数。用户已提供 Key，并已对30秒样片执行一次在线请求；接口返回 HTTP 500，业务错误为当前 Token Plan 不支持 asr-1.0（2061），因此尚未成功转写。密钥未写入项目文件。

错误记录位于 output/audio/ta2a/batman-ta2a-sample.minimax.error.json。临时可用的逐字字幕位于同目录 .seed.words.json / .srt / .vtt，来源为 Seed Audio 生成响应，不是 MiniMax 独立识别。零时长标点在字幕导出时附加到前一个字，原始时间戳保留在 JSON 中。

```bash
python3 scripts/minimax_transcribe.py output/audio/ta2a/batman-ta2a-sample.wav
```

输出原始 .minimax.json（保留 speaker 和时间戳），以及 .minimax.words.srt / .minimax.words.vtt。逐字字幕用于精确同步；连续阅读可另行按句分组。不要自动将 S1/S2 当作固定角色身份，也不要把说话人数当作音色一致性的证明。独立 ASR 结果可与生成脚本对比，检查漏读、增词及角色切换。

官方文档：https://platform.minimax.io/docs/api-reference/speech-to-text

后续用户反馈：咬字修正版末句仍然过于平静。`scene-02-anger-v4-prompt.txt` 保持无逐句时间窗口，末句改为明确的大声怒喝、短促爆发、胸腔发力，要求怒意鲜明且发音完整；避免用逐字拖长或嘶吼变形来制造愤怒。继续整段生成并保留旧版，情绪验收以试听为准。
